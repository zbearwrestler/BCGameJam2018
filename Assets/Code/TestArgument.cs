using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestArgument : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Debug.Log(ArgumentText.GetLine("Angery", 0, 0));
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
